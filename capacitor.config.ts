import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.easepos.app',
  appName: 'EasePOS',
  webDir: 'dist/ease-pos',
  bundledWebRuntime: false
};

export default config;
